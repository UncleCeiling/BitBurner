/**
 * Takes an input sting and returns the 
 * @param {String} input Some string
 * @returns A message containing the string.
 */
export function an_example(input) {
    return `You passed "${input}" into the example function.`
}